box: python:3.5

dev:
  steps:
    - pip-install
    - internal-watch:
      code: nosetests
      reload: true

build:
  steps:
    - pip-install
    - script:
      name: tests
      code: nosetests
